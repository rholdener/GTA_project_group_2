# GTA Project Group 2

This project is a Flask-based web application that provides various endpoints to interact with a PostgreSQL database. The application is designed to handle data related to air pollution, noise levels, and tree counts in the GTA area.

## Prerequisites
- "gta24" conda Enviroment

## Running the Application

To start the Flask application, follow these steps:

1. Activate the conda enviroment
2. Navigate to the project directory:
3. Start the Flask application:

The application should now be running, and you can access it via your web browser at `http://localhost:8989`.

## Endpoints

- **`GET /test_deploy`**: Test the deployment of the app.
- **`GET /`**: Test the connection to the server.
- **`GET /test_data`**: Fetch data from the database.
- **`GET /point_history`**: Get all points of a trip.
- **`GET /calculate_ri`**: Calculate the RI value, noise value, and tree distance for a given location.
- **`GET /highest_trip_id`**: Returns the highest trip_id in the database.
- **`GET /login`**: Login a user.
- **`GET /register`**: Register a new user.
- **`GET /insert_trip`**: Insert a new trip into the database.
- **`GET /get_trips`**: Get trips for a user.
- **`POST /update_mean_ri`**: Update the mean RI of a trip.
- **`GET /city_ri`**: Get the mean RI value of a city.